# Paper Summarizer — Frontend

React + TypeScript + Vite frontend for the Paper Summarizer app.

## Setup

Copy `.env.example` to `.env` and set `VITE_API_URL` to your backend URL.

```bash
npm install
npm run dev
```

## Stack

- React + TypeScript
- Vite
- Tailwind CSS
- Axios
